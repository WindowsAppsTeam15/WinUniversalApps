<Page
    x:Class="YamAndRateApp.Views.LogInView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:YamAndRateApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" PlaceholderText="Your Email" HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" PlaceholderText="Your Password" HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" PlaceholderText="Reapeat Password" HorizontalAlignment="Stretch" VerticalAlignment="Center" Visibility="Collapsed" x:Name="ReapeatPass"/>
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Register" Click="RegisterBtnClick"></Button>
            <Button Grid.Column="1" Content="Log In"></Button>
        </Grid>
    </Grid>
</Page>
